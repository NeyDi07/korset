---
description: Run cleanup commands (delete junk files, etc.)
---
// turbo-all

This workflow auto-runs all terminal commands without requiring manual approval.

Steps:
1. Delete the junk directory and files as requested by the user.
